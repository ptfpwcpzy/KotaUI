# KotaUI 本地界面验证记录

- 预览地址：`http://127.0.0.1:21000/ptf`
- 验证时间：2026-08-16
- 登录页可正常加载，显示 KotaUI 标题、中文登录提示、账号字段、密码字段和登录按钮。
- 轻量浅色卡片布局与字体层级正常；尚需登录后继续检查概览页与新增操作。

登录后概览页可正常加载，左侧导航、浅色卡片、CPU/内存/磁盘指标、证书状态、面板 URL 与产品提示均清晰可见。入站页的导航切换、新增入口和“需先创建入站”的空状态提示均正常显示。

新增入站对话框在桌面宽度下布局完整。VLESS + REALITY 显示目标检测、密钥对、short_id 与 SNI 字段；切换到 Hysteria 2 后，表单正确替换为上下行速率、TLS server_name 与可选混淆密码字段。

在隔离预览中创建 Hysteria 2 入站后，表单能返回列表，列表正确显示名称、协议、端口、UDP 网络、启用状态和删除操作，并显示“入站已创建”反馈。

客户端页在已有入站后自动启用新增按钮。新增客户端对话框正确列出可选入站，并同时展示总流量、自然月流量、有效期和同时在线 IP 数四项用户级控制字段。

在隔离预览中创建客户端后，列表正确显示已选入站数量、GiB 换算后的总流量与自然月流量、有效期、绿色正常状态，以及订阅、暂停、重置、删除操作。中文管理员备注正常显示。

